{
  "name": "spex",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [ ".git", "*" ],
  "scripts": {
    "postinstall": "mkdir .git/refs; git checkout .gitignore"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.3.0"
  }
}
